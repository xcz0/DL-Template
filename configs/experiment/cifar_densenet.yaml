# @package _global_
# configs/experiment/cifar_densenet.yaml
# CIFAR-10 DenseNet 实验配置

defaults:
  - override /data: cifar10
  - override /model: cifar_densenet

tags: ["cifar10", "densenet"]

trainer:
  max_epochs: 200

callbacks:
  early_stopping:
    patience: 20
