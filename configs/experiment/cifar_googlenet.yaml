# @package _global_
# configs/experiment/cifar_googlenet.yaml
# CIFAR-10 GoogleNet 实验配置

defaults:
  - override /data: cifar10
  - override /model: cifar_googlenet

tags: ["cifar10", "googlenet"]

trainer:
  max_epochs: 200

callbacks:
  early_stopping:
    patience: 20
