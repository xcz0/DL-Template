# @package _global_
# configs/experiment/cifar_resnet.yaml
# CIFAR-10 ResNet 实验配置

defaults:
  - override /data: cifar10
  - override /model: cifar_resnet

tags: ["cifar10", "resnet"]

trainer:
  max_epochs: 200

callbacks:
  early_stopping:
    patience: 20
