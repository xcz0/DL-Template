# @package _global_
# configs/experiment/cifar_resnet_preact.yaml
# CIFAR-10 Pre-Activation ResNet 实验配置

defaults:
  - override /data: cifar10
  - override /model: cifar_resnet_preact

tags: ["cifar10", "resnet_preact"]

trainer:
  max_epochs: 200

callbacks:
  early_stopping:
    patience: 20
